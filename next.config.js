/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["api.zuraaa.com", "127.0.0.1", "media.discordapp.net"]
  }
}

module.exports = nextConfig
